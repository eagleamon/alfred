TODO's

- Don't log to files etc at init, but in start if defined in host config (sd wear)
- multiple netio (and others) -> config
- Don't register all items two times, and only items for binding
- handle devices for swap: in swap folder, svn co when needed
- Handle bower components nicely -> create a webclient build for publishing
- Put xbmc web interface control in media page
- Change angular ellipsis for binding page binding column
- edition web intf des item commands et des groupes
- general command to restart all nodes i.e.
- health monitor of all instances from all instances (topic?)
- goto PYTHON3
- create a debian package

- publish alfred on pip
- create a freeze (just in case)
